// issuerecord_save(), issuerecord_update(), issuerecord_find_last_by_id(copy_id)
#include <stdio.h>
#ifndef ___ISSUERECORD.H
#define ___ISSUERECORD .H
#define "issuerecord.db"

void issuerecord_save(issuerecord_t *r);
void issuerecord_update();
void issuerecord_find_by_id();
#endif